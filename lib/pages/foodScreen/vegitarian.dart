import 'package:flutter/material.dart';

class Vegetarian extends StatelessWidget {
  const Vegetarian({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}