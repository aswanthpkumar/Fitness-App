import 'package:flutter/material.dart';

class HeadTilt extends StatelessWidget {
  const HeadTilt({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}